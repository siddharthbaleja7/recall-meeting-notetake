# Claude Code entry point

Full instructions live in `AGENTS.md` at repo root — read that first, then
`docs/MVP.md`, `docs/DESIGN.md`, `docs/DATA-MODEL.md` in that order.

Do not skip step 0 in `AGENTS.md` (capture logging verification) before writing any
product code.
